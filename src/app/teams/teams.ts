import { Component } from '@angular/core';

@Component({
  selector: 'app-teams',
  standalone: true,
  templateUrl: './teams.html',
  styleUrl: './teams.scss',
})
export class Teams {
  // Voorlopig geen ingewikkelde data nodig!
}
